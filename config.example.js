// Copy this to config.js and fill in your values
const CONFIG = {
    OPENAI_PROXY_URL: 'http://localhost:8004', // Change to Railway URL in production
    N8N_ORDER_WEBHOOK_URL: 'https://your-make-webhook-url.com',
    DEMO_MODE: false
};

